div.ui-widget-content.ui-corner-all.contacts
    h3.ui-widget-header.ui-corner-all New Address for #{contact.name}

    - if (error)
        div.ui-state-error.ui-corner-all
            span.ui-icon.ui-icon-alert
            #{error}

    p Adding a new address of an existing type will not generate an error, it will overwrite the current one. So beware!
    div
        form(method="post")
            fieldset
                legend new address
                p
                    label(for="addressType") Address type
                        select(name="addressType")
                            option(value="PRIVATE") private
                            option(value="WORK") work
                            option(value="VACATION") vacation
                p
                    label(for="streetAndNumber") Street and number
                        input(type="text", name="streetAndNumber", size="50")
                p
                    label(for="zipCode") Zip code
                        input(type="text", name="zipCode", size="10")
                p
                    label(for="city") City
                        input(type="text", name="city", size="50")
            p.buttons
                input(type="submit", value="Create")
